const colors = require('./src/colors.ts');
const fonts = require('./src/fonts.ts');
const radii = require('./src/radii.ts');
const spacing = require('./src/spacing.ts');

module.exports = {
  colors,
  fonts,
  radii,
  spacing
};
